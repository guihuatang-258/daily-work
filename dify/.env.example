# 从已登录哪吒平台的浏览器请求中复制完整 Cookie。
# Cookie 必须包含 __Host-csrf_token。
NEZHA_COOKIE=""

# 当 config.yaml 的 authentication.type 为 authorization 时，
# 从浏览器请求头复制完整 Authorization 值，例如 Bearer eyJ...。
DIFY_AUTHORIZATION=""
