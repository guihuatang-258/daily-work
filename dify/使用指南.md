# Dify 日志查询工具入门指南

这是一款运行在命令行中的 Dify Console 辅助工具，可以查询 Workflow 日志、统计 Token 消耗，以及检查业务组中当天失败的 Run。

本文以 Windows 电脑和 PowerShell 为例。即使没有 Python 使用经验，也可以按顺序完成安装和使用。

## 一、开始前需要准备什么

请先确认你拥有：

1. 一台 Windows 电脑。
2. 能正常登录目标 Dify Console 的账号。
3. Python 3.10 或更高版本。
4. 本项目的代码文件。

检查 Python 是否已经安装：

1. 按 `Win + R`。
2. 输入 `powershell`，按回车。
3. 在打开的窗口中输入：

```powershell
python --version
```

如果显示类似 `Python 3.12.4`，说明已经安装。只要版本不低于 3.10 即可。

如果提示找不到 `python`，请前往 [Python 官方网站](https://www.python.org/downloads/)下载安装。安装时务必勾选 **Add Python to PATH**。

## 二、下载项目

如果已经拿到项目文件，可以跳到“安装依赖”。

使用 Git 下载：

```powershell
git clone https://github.com/guihuatang-258/daily-work.git
cd daily-work\dify
```

也可以在 GitHub 页面下载 ZIP 压缩包，解压后进入其中的 `dify` 文件夹。在文件夹空白处按住 `Shift` 并单击鼠标右键，选择“在此处打开 PowerShell 窗口”。

后面的所有命令都要在 `dify` 文件夹中执行。可以输入下面的命令确认当前位置：

```powershell
Get-Location
```

显示的路径末尾应当是 `daily-work\dify`。

## 三、安装依赖

### 1. 创建独立的 Python 环境

```powershell
python -m venv .venv
```

这条命令会在当前目录创建 `.venv` 文件夹，避免本工具的依赖影响电脑上的其他 Python 项目。

### 2. 安装依赖

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

看到 `Successfully installed` 表示安装成功。以后运行工具时，直接使用 `.venv` 中的 Python，不需要每次激活虚拟环境。

如果下载速度慢，可以稍后重试，或联系网络管理员确认是否需要设置代理。

## 四、配置 Dify 地址

用记事本或代码编辑器打开当前目录中的 `config.yaml`，找到：

```yaml
instance:
  name: nezha-production
  base_url: https://你的-dify-地址.example.com
  timezone: Asia/Shanghai
```

需要关注的内容：

- `name`：给当前 Dify 环境起一个便于识别的名称。
- `base_url`：浏览器中访问 Dify Console 使用的地址，不要在末尾添加 `/`。
- `timezone`：统计“今天”“昨天”等时间范围时使用的时区，中国大陆通常保持 `Asia/Shanghai`。

初次使用时，其他参数可以保持默认值。

> YAML 对空格比较敏感。请保留字段前面的空格，不要使用 Tab 键缩进。

## 五、配置登录 Cookie

工具不会要求输入 Dify 的账号和密码，而是使用浏览器登录后产生的 Cookie 访问 Console。

### 1. 创建 `.env` 文件

在 PowerShell 中执行：

```powershell
Copy-Item .env.example .env
```

### 2. 从浏览器复制完整 Cookie

以 Chrome 或 Edge 为例：

1. 在浏览器中登录 Dify Console。
2. 按 `F12` 打开开发者工具。
3. 选择 **网络（Network）** 面板。
4. 刷新 Dify 页面。
5. 在请求列表中选择一个地址包含 `/console/api/` 的请求，例如 `apps` 请求。
6. 在右侧选择 **标头（Headers）**。
7. 找到 **请求标头（Request Headers）** 中的 `Cookie`。
8. 复制 `Cookie:` 后面的完整内容，不要只复制其中某一项。

正确的 Cookie 通常包含多个由分号分隔的字段，并且必须包含：

```text
__Host-csrf_token=...
```

### 3. 保存 Cookie

打开 `.env`，把完整 Cookie 粘贴到双引号中：

```env
NEZHA_COOKIE="这里粘贴完整 Cookie"
```

保存文件后关闭编辑器。

> Cookie 相当于临时登录凭证。不要发送给其他人，不要截图分享，也不要提交到 Git 仓库。

## 六、第一次运行

在 `dify` 文件夹中执行：

```powershell
.\.venv\Scripts\python.exe analyze_nezha_apis.py
```

认证成功后，会看到类似菜单：

```text
┌─ Dify 日志查询工具
│  [1] 查询 · Workflow
│  [2] 查询 · Chatflow
│  [3] 检查 · 失败的 Run
│  [4] 统计 · 示例组 Token 消耗
└─ [0] 退出
```

输入编号后按回车即可。直接按回车等同于选择 `1`。

如果程序提示粘贴最新 Cookie，可直接在窗口中粘贴并按回车。出于安全考虑，输入内容不会显示，这是正常现象。程序验证成功后会自动更新 `.env`。

## 七、各项功能怎么用

### 查询 Workflow 日志

1. 在主菜单输入 `1`。
2. 输入 Workflow 名称中的一部分；直接回车会显示全部 Workflow。
3. 输入应用前面的编号。
4. 选择“查看最近日志”或“统计最近 Token 消耗”。
5. 查看日志时，还可以继续选择某一条运行记录查看详情。

查看运行详情后，完整响应会保存为当前目录下的 `workflow_run_运行ID.json` 文件。该文件可能包含业务输入和输出，分享前请检查敏感信息。

### 查询 Chatflow Token 消耗

1. 在主菜单输入 `2`。
2. 输入 Chatflow 名称关键字，或者直接回车显示全部。
3. 选择一个应用。
4. 选择统计范围：今天、昨天、最近 3 天或最近 7 天。

### 检查失败的 Run

1. 在主菜单输入 `3`。
2. 选择“全部分组”，或选择某一个业务组。
3. 等待检查完成。

报告会列出检查的应用数、失败 Run 数量和失败应用。看到“未发现失败运行”表示所选范围内没有查询到失败记录。

### 统计整个业务组的 Token

主菜单中从编号 `4` 开始的选项来自业务组配置。例如输入 `4`，就会统计菜单中对应业务组的 Token 消耗。

结果中带 `~` 的数字是根据样本估算的近似值，不代表精确总量。

### 退出程序

在主菜单输入 `0`。在部分子菜单中，直接按回车可以取消当前操作并返回上一级。

## 八、配置业务组

业务组用于批量统计 Token 和检查失败 Run。

第一次配置时，可以复制示例文件：

```powershell
Copy-Item dify_flow_groups.example.json dify_flow_groups.json
```

然后编辑 `dify_flow_groups.json`：

```json
{
  "groups": {
    "customer_service": {
      "display_name": "客服组",
      "apps": [
        {
          "name": "客服问答",
          "app_id": "00000000-0000-0000-0000-000000000001",
          "mode": "advanced-chat"
        },
        {
          "name": "工单摘要",
          "app_id": "00000000-0000-0000-0000-000000000002",
          "mode": "workflow"
        }
      ]
    }
  }
}
```

字段含义：

| 字段 | 说明 | 示例 |
|---|---|---|
| `customer_service` | 分组代号，命令行检查时使用 | `customer_service` |
| `display_name` | 主菜单和报告中显示的名称 | `客服组` |
| `name` | 应用名称，仅用于报告展示 | `客服问答` |
| `app_id` | Dify 应用的 UUID | `00000000-...` |
| `mode` | 应用类型 | `workflow`、`advanced-chat` 或 `chat` |

应用 ID 通常可以从 Dify 应用编辑页面的网址中找到。例如网址包含 `/app/1234.../workflow`，其中 `1234...` 就是应用 ID。

注意：

- JSON 中的字段名和文字必须使用英文双引号。
- 同一组中的每个应用之间要有英文逗号。
- 最后一项后面不能多写逗号。
- `dify_flow_groups.json` 可能包含内部应用名称和 ID，不要公开分享。

保存后重新启动程序，新分组会自动出现在主菜单中。

## 九、使用 `--check-failures` 快速检查

不进入菜单，直接检查某个业务组当天的失败 Run：

```powershell
.\.venv\Scripts\python.exe analyze_nezha_apis.py --check-failures customer_service
```

一次检查多个组时，用空格分隔分组代号：

```powershell
.\.venv\Scripts\python.exe analyze_nezha_apis.py --check-failures customer_service knowledge_search
```

这里使用的是 `dify_flow_groups.json` 中 `groups` 下的分组代号，不是 `display_name`。

### Cookie 过期时会发生什么

在 Windows 上运行 `--check-failures` 时，如果检测到 Cookie 过期：

1. 程序会自动打开一个新的 Cookie 输入窗口。
2. 在新窗口中粘贴完整 Cookie，然后按回车。
3. 输入内容不会显示，这是正常的安全设计。
4. 验证成功后，新窗口会自动关闭，无需手动关闭。
5. 原窗口会自动继续检查失败 Run。

如果 Cookie 格式错误或验证失败，新窗口会保留错误提示。按照提示重新输入；如果最终失败，按回车关闭窗口后重新运行命令。

> Linux 和 macOS 当前不会自动打开新的 Cookie 窗口。请先在 `.env` 中更新 Cookie，再重新执行检查命令。

## 十、可选：调整统计配置

普通用户可以保持 `config.yaml` 中的默认设置。遇到速度或请求频率问题时，再考虑调整以下参数：

| 参数 | 作用 | 调整建议 |
|---|---|---|
| `collection.apps_page_size` | 每次读取多少个应用 | 通常保持默认值 |
| `collection.interactive_log_limit` | 菜单中显示多少条最近日志 | 数字越大，等待时间可能越长 |
| `collection.monitor_page_size` | 失败检查每页读取数量 | 通常保持默认值 |
| `collection.token_stats_workers` | 同时统计多少个应用 | 遇到 HTTP 429 时调小 |
| `token_statistics.workflow_sample_size` | Workflow Token 估算样本量 | 越大越接近实际值，但查询更慢 |

修改正整数参数时必须填写大于 `0` 的整数。

## 十一、常见问题

### 提示“缺少 `__Host-csrf_token`”

复制的 Cookie 不完整。请从 `/console/api/` 请求的 Request Headers 中重新复制完整 Cookie。

### 提示 HTTP 401 或 HTTP 403

Cookie 已过期、Cookie 与 CSRF 信息不匹配，或者当前账号没有访问该应用的权限。先确认浏览器仍能正常打开 Dify，再重新复制 Cookie。

### 提示 HTTP 429

请求太频繁。等待一段时间后重试，并适当调小 `config.yaml` 中的 `collection.token_stats_workers`。

### 提示找不到业务组

检查命令中填写的是否为分组代号。例如配置是 `"customer_service": {...}`，命令中就要写 `customer_service`，不能写显示名称“客服组”。

### 主菜单中找不到某个应用

可能的原因：

- 输入的名称关键字不匹配。
- 当前登录账号没有该应用的查看权限。
- 应用类型与所选菜单不一致，例如 Chatflow 应用不会出现在 Workflow 列表中。

### PowerShell 不允许激活虚拟环境

本指南中的命令直接运行 `.venv\Scripts\python.exe`，不需要激活虚拟环境，因此不受 PowerShell 执行策略影响。

### 中文显示乱码

建议使用 Windows Terminal 或新版 PowerShell。也可以先执行：

```powershell
chcp 65001
```

然后重新运行工具。

### 程序一闪而过

不要双击 `analyze_nezha_apis.py`。请先打开 PowerShell，进入 `dify` 文件夹，再输入本文中的运行命令，这样才能看到完整错误信息。

## 十二、安全提醒

- `.env` 和 Cookie 都是敏感信息，不要上传、转发或截图分享。
- 不要把包含真实应用名称和 ID 的 `dify_flow_groups.json` 发到公共场合。
- 导出的运行详情 JSON 可能包含用户输入和模型输出，发送前必须检查内容。
- 离开电脑或不再使用工具时，应退出 Dify 账号，让旧 Cookie 失效。

## 十三、最短操作清单

已经完成配置后，每天只需要：

```powershell
cd 你的项目路径\daily-work\dify
.\.venv\Scripts\python.exe analyze_nezha_apis.py
```

如果只想检查失败 Run：

```powershell
.\.venv\Scripts\python.exe analyze_nezha_apis.py --check-failures 分组代号
```

遇到 Cookie 过期，按窗口提示粘贴新 Cookie 即可。验证成功后窗口会自动关闭，检查会继续执行。
